import unit from "./unit";

const preset = {
  fiveSec: 5 * unit.second,
  tenSec: 10 * unit.second,
  oneMin: unit.minute,
  fiveMin: 5 * unit.minute,
  tenMin: 10 * unit.minute,
  fifteenMin: 15 * unit.minute,
};

export default preset;
